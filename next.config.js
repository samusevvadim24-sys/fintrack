/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow the development phone/browser origin to load Next.js dev resources.
  allowedDevOrigins: ["192.168.113.126"],
};

module.exports = nextConfig;
